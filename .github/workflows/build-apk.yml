name: Build Android APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.3.1' 

      - name: Generate Dummy Debug Keystore
        run: |
          keytool -genkey -v -keystore debug.keystore -storepass android -alias androiddebugkey -keypass android -keyalg RSA -keysize 2048 -validity 10000 -dname "CN=Android Debug,O=Android,C=US"

      # บิลด์ในโหมดมาตรฐาน ปล่อยให้ KSP2 ทำงานแบบประหยัดแรมเพื่อไม่ให้ระบบ IntelliJ เอ๋อ
      - name: Build Debug APK
        env:
          GRADLE_OPTS: "-Dorg.gradle.jvmargs=-Xmx4g -Djava.awt.headless=true"
        run: gradle assembleDebug -x lint --no-build-cache --no-configuration-cache
        
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: AppChat-Debug-APK
          path: app/build/outputs/apk/debug/app-debug.apk
